{
  "name": "practicatdd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: Shoul run test with command npm run testpush\" && exit 1",
    "testpush": "jest && git add . && git commit -m 'TDD iteration' && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.1.2"
  }
}
